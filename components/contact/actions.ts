'use server';

export const handleSendEmail = async () => {
  try {
    // const data = await sendEmail(
    //
    // );
    // console.log(data);
  } catch (error) {
    console.error(error);
  }
};
